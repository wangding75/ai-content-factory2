# AI Content Factory 2.0
## Iteration 12 遗留技术债记录

- **记录日期**：2026-07-20
- **来源迭代**：Iteration 12
- **最终代码基线**：`ab32dfdeae16079861672cde2123b5bbfa4086b0`
- **当前决策**：Iteration 12 不再因以下事项阻塞，项目进入下一阶段开发
- **处理原则**：后续集中进入质量修复迭代，不在新功能开发中顺手扩大修改范围

---

## 1. 结论

Iteration 12 的核心业务功能、前后端联调、真实 Docker 环境、自动化测试和主要 UI 页面已经达到进入下一阶段的条件。

当前未发现阻塞下一阶段开发的 P0/P1 业务代码缺陷。以下问题统一登记为技术债，后续按优先级集中修复。

---

## 2. 技术债清单

| ID | 问题 | 类型 | 优先级 | 影响 | 后续处理建议 | 验收标准 |
|---|---|---|---|---|---|---|
| TD-12-01 | Workflow 未真实更换连接时，可能仍记录 `connection_rebind` Audit | 后端审计语义 | P2 | 审计记录准确性下降，但不影响业务保存结果 | 比较更新前后 `connectionId`，仅在实际变化时记录 `connection_rebind` | 仅修改名称、备注或默认参数时不产生 `connection_rebind`；真实换绑时只产生一次 |
| TD-12-02 | PATCH 请求提交完整表单，语义接近 PUT | API/前端设计 | P2 | 未变化字段也可能参与更新，增加版本递增、Audit 和并发冲突概率 | 前端构造差异化 PATCH；后端保持字段级可选更新 | 未修改字段不进入请求；无变化提交不产生无意义更新 |
| TD-12-03 | Workflow 更新时连接存在性查询未完全使用同一事务 | 后端事务一致性 | P3 | 当前无连接删除接口，现实风险较低；未来扩展后可能产生竞态 | 将连接检查纳入当前 transaction/repository | 连接校验、Workflow 更新和 Audit 写入处于同一事务边界 |
| TD-12-04 | 实机 UI 与冻结原型存在轻微视觉偏差 | UI 还原 | P2 | 不影响功能，但未达到像素级一致 | 集中调整间距、行高、列宽、抽屉宽度、状态标签和信息密度 | 同尺寸截图逐项比对；关键布局无明显偏差 |
| TD-12-05 | 部分空态未在真实数据库环境中截图验证 | UI 验收 | P3 | 空态已有代码，但缺少真实运行证据 | 使用隔离测试数据或专用测试环境验证，不删除用户数据 | 四个模块空态均有真实截图和交互结果 |
| TD-12-06 | 缺少完整的浏览器 E2E 自动化闭环 | 测试体系 | P2 | 创建、编辑、冲突恢复等流程仍依赖人工回归 | 增加 Playwright E2E，覆盖四个设置模块核心流程 | 自动覆盖列表、筛选、分页、新建、编辑、409、字段错误和刷新持久化 |
| TD-12-07 | 审核包导出脚本未自动收集真实 UI 截图 | 工程流程 | P2 | 首次导出遗漏截图，需要人工二次打包 | 将 `actual-ui` 纳入固定导出目录并校验数量 | 导出脚本自动包含 12 张截图；缺图时直接失败 |
| TD-12-08 | 审核包内 ZIP 文件数、文件名和 SHA 自校验可能仍引用旧包 | 工程流程 | P2 | 审核包内部信息与最终 ZIP 不一致，降低可审计性 | 在 ZIP 最终生成后重新计算并写入校验报告 | 包内校验文件与最终 ZIP 的名称、文件数、大小和 SHA-256 完全一致 |
| TD-12-09 | `ui-frame-mapping.md` 映射粒度不足 | 验收文档 | P2 | 无法逐页面追踪冻结 Frame、实现组件、实机截图和实际偏差 | 按列表、新建、编辑、异常状态逐项映射 | 每个 Frame 均对应路由、组件、截图、结论和真实偏差 |
| TD-12-10 | 真实联调、幂等、409、Audit、日志和持久化证据未形成统一标准报告 | 质量证据 | P2 | 执行过程通过，但审核者难以仅依靠包内文件复核 | 建立固定 evidence 模板并由脚本收集 | 审核包包含联调矩阵、幂等证据、409、字段错误、Audit 脱敏、Console、Network、API/Web 日志和数据库证据 |

---

## 3. 建议修复顺序

### 近期质量修复

1. **TD-12-01：修复 `connection_rebind` Audit 误记录**
2. **TD-12-07：审核包自动收集真实 UI 截图**
3. **TD-12-08：审核包最终 SHA 和文件清单自校验**
4. **TD-12-09：完善 UI Frame 映射**
5. **TD-12-10：统一最终验收证据模板**

### 后续工程优化

1. TD-12-02：PATCH 差异化提交
2. TD-12-03：事务边界统一
3. TD-12-06：补齐 Playwright E2E
4. TD-12-04：UI 像素级优化
5. TD-12-05：空态真实环境验证

---

## 4. 不阻塞下一阶段的依据

| 检查项 | 当前结论 |
|---|---|
| 核心后端功能 | 通过 |
| 四个配置模块前端功能 | 通过 |
| 真实 API / PostgreSQL | 已联调 |
| Docker API/Web | 已重建并健康运行 |
| Web 自动化测试 | 103/103 通过 |
| 后端定向与 PostgreSQL 集成测试 | 通过 |
| ESLint / Typecheck / Production Build | 通过 |
| 冻结契约校验 | 通过 |
| 浏览器四路由检查 | 通过 |
| 真实 UI 截图 | 12 张已采集 |
| 阻塞性代码缺陷 | 未发现 |

---

## 5. 后续治理规则

- 技术债必须通过独立任务处理，不与下一阶段新功能混合提交。
- 每项修复必须关联技术债 ID，例如：`TD-12-01`。
- 修复前先补充可复现测试，修复后执行局部验证，再执行相关总门禁。
- UI 技术债必须提供同尺寸的冻结原型与实机截图对照。
- 审核包流程修复后，应使用脚本重新生成，不再手工向旧 ZIP 追加文件。
- 技术债关闭时更新状态、Commit、验证证据和关闭日期。

---

## 6. 状态定义

| 状态 | 含义 |
|---|---|
| OPEN | 已登记，尚未处理 |
| IN_PROGRESS | 已进入专门修复任务 |
| VERIFYING | 已修复，等待自动化或人工验收 |
| CLOSED | 修复和证据均通过 |
| WONT_FIX | 明确评估后不再处理，并记录原因 |

当前 TD-12-01 至 TD-12-10 初始状态均为 **OPEN**。
