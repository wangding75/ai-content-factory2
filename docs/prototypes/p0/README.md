# P0 UI 冻结稿入口

冻结 HTML、可点击页面与截图按迭代存放：

```text
docs/development-inputs/p0/iterations/<iteration>/ui/frames/<FRAME_ID>/
├── code.html
├── index.html
└── screen.png
```

实现允许组件化重构，但必须保留页面业务含义、字段、状态、导航入口和操作结果。
