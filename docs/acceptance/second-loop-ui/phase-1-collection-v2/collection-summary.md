# 第二闭环 UI 完整验收第一阶段证据重采汇总报告 (Summary V2)

## 一、整体统计数据
- **总采集项数**：88 项
- **COLLECTED (成功采集数)**：9 项
- **NOT_REPRODUCIBLE (不可复现数)**：79 项
- **ROUTE_MISMATCH (路由不匹配数)**：0 项
- **INVALID_CAPTURE (无效截图数)**：0 项
- **实际保存 PNG 图片数量**：9 张
- **Mock 机制使用次数**：0 次
- **Secret 泄露数**：0 项

## 二、业务区域细分统计
| 业务区域 | 总项数 | COLLECTED | NOT_REPRODUCIBLE |
|---|---|---|---|
| 全局配置 | 12 | 4 | 8 |
| 项目绑定 | 9 | 1 | 8 |
| 流程运行 | 10 | 3 | 7 |
| 章节规划 | 22 | 1 | 21 |
| 故事线 | 1 | 0 | 1 |
| 正文生成 | 13 | 0 | 13 |
| 内容审核 | 10 | 0 | 10 |
| 正文重写 | 11 | 0 | 11 |
| 共享升级 | 8 | 0 | 8 |

## 三、重复截图哈希审查 (Duplicate Hash Review)
- **审查结论**：无任何重复哈希截图。所有 COLLECTED 项均对应唯一的真实独立界面。

## 四、不可复现项列表 (Not Reproducible Items)
### 1. [UI-002] 全局设置 / LLM Provider / 新增/编辑抽屉
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-002 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-003] 全局设置 / LLM Provider / 空状态
- **断言未成立原因**：未能在页面中找到 '暂无 LLM 配置' 和 '添加一个 LLM 配置后' 等空状态文案
- **实际观察状态**：页面渲染了已有的 'GPT-4o Production' 和 'Claude 3.5 Sonnet' 两个大模型配置列表
- **缺少的数据依赖**：缺少配置为空的大模型服务状态
- **推荐的测试夹具**：需要删除数据库中所有的 llm_provider_configurations 记录
- **跟进行动**：在测试或过渡环境下清空全局大模型配置，或创建一个完全隔离的新租户以呈现空状态

### 1. [UI-004] 全局设置 / n8n Connection / 列表
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-004 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-006] 全局设置 / n8n Connection / 空状态
- **断言未成立原因**：未能在页面中找到 'Manage reusable connections' 空状态提示或无数据展现
- **实际观察状态**：页面渲染了已有的连接通道列表
- **缺少的数据依赖**：缺少配置为空的外部通道连接状态
- **推荐的测试夹具**：需要删除数据库中所有的 workflow_connections 记录
- **跟进行动**：在测试数据库中清空 workflow_connections，或筛选无连接的连接通道类别以呈现空状态

### 1. [UI-007] 全局设置 / Workflow Configuration / 列表
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-007 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-008] 全局设置 / Workflow Configuration / 新增/编辑抽屉
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-008 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-009] 全局设置 / Workflow Configuration / 空状态
- **断言未成立原因**：未能在页面中找到 '暂无工作流配置' 或无数据展现
- **实际观察状态**：页面正常渲染了已有的大批工作流配置项列表
- **缺少的数据依赖**：缺少配置为空的工作流大纲配置状态
- **推荐的测试夹具**：需要删除数据库中所有的 workflow_configurations 记录
- **跟进行动**：在测试环境中清空 workflow_configurations 记录

### 1. [UI-012] 全局设置 / 分发平台 / 空状态
- **断言未成立原因**：未能在页面中找到 '暂无分发平台配置' 提示
- **实际观察状态**：页面正常渲染了已有的分发平台渠道配置
- **缺少的数据依赖**：缺少配置为空的分发平台配置状态
- **推荐的测试夹具**：需要删除数据库中所有的 distribution_platform_configurations 记录
- **跟进行动**：清空 distribution_platform_configurations 表记录以呈现空列表

### 1. [UI-013] 项目设置 / 工作流绑定 / 四阶段全部未绑定
- **断言未成立原因**：未能在页面中找到 '4阶段全部未绑定' 或 '未绑定' 等状态指示
- **实际观察状态**：页面显示该项目工作流已全部绑定，状态为 4/4 已绑定
- **缺少的数据依赖**：缺少项目工作流四阶段全部未绑定的状态
- **推荐的测试夹具**：需要删除项目 10000000-0000-4000-8000-000000000001 在 project_workflow_bindings 中的所有绑定记录
- **跟进行动**：在项目设置的工作流绑定管理中，手动将四个阶段全部解除绑定

### 1. [UI-014] 项目设置 / 工作流绑定 / 部分已绑定
- **断言未成立原因**：未能在页面中找到 '部分已绑定' 等状态指示
- **实际观察状态**：页面显示 4/4 全部已绑定状态
- **缺少的数据依赖**：缺少项目工作流部分阶段已绑定的状态
- **推荐的测试夹具**：需要只保留部分阶段绑定，删除其他绑定记录
- **跟进行动**：在项目设置的工作流绑定管理中，手动解除一至三个阶段的绑定

### 1. [UI-016] 项目设置 / 工作流绑定 / 绑定保留但依赖失效
- **断言未成立原因**：未能在页面中找到 '依赖失效' 或 '配置失效' 等警示标志
- **实际观察状态**：项目处于全部已绑定且可执行的健康状态
- **缺少的数据依赖**：缺少项目工作流绑定所依赖的大模型服务或通道被删除的状态
- **推荐的测试夹具**：需要删除已绑定工作流在全局配置中所指向的大模型服务提供商或通道连接
- **跟进行动**：清空或删除对应绑定工作流的后端模型服务以触发失效校验

### 1. [UI-017] 项目设置 / 工作流绑定 / 首次选择工作流抽屉
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-017 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-018] 项目设置 / 工作流绑定 / 更换工作流抽屉
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-018 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-019] 项目设置 / 工作流绑定 / 解除绑定确认弹窗
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-019 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-020] 项目设置 / 工作流绑定 / 当前阶段无可用工作流
- **断言未成立原因**：未能在更换工作流时找到 '无可用工作流' 的提示
- **实际观察状态**：页面显示了可选用的全局工作流大纲列表
- **缺少的数据依赖**：缺少当前阶段无任何适用全局工作流可被选用的状态
- **推荐的测试夹具**：需要删除全局配置中适用于当前选定阶段的所有 workflow_configurations
- **跟进行动**：在全局配置中暂时停用或删除对应阶段的工作流模板

### 1. [UI-021] 项目设置 / 工作流绑定 / 保存并发冲突
- **断言未成立原因**：未能在页面中找到 '保存冲突'、'他人已更新' 或者是 '并发修改失败' 等提示
- **实际观察状态**：单客户端正常访问，保存无并发报错
- **缺少的数据依赖**：缺少多端或多请求并发提交绑定修改而爆发并发冲突的状态
- **推荐的测试夹具**：需要两个客户端同时发起写入 project_workflow_bindings 锁定的请求，引发冲突报错
- **跟进行动**：通过 API 测试工具模拟并发写冲突

### 1. [UI-024] 流程中心 / WorkflowRun / 列表空状态
- **断言未成立原因**：未能在列表中找到 '暂无运行记录' 的空状态说明
- **实际观察状态**：全局列表中有大量历史运行记录
- **缺少的数据依赖**：缺少工作流运行记录列表为空的状态
- **推荐的测试夹具**：需要清空数据库中所有的 workflow_runs 和 workflow_run_records 记录
- **跟进行动**：在干净的测试数据库中访问此页面

### 1. [UI-025] 流程中心 / WorkflowRun / 列表错误状态
- **断言未成立原因**：未能在页面中看到错误提示、重试按钮或异常覆盖层
- **实际观察状态**：后端 API 完美返回运行记录，列表显示正常
- **缺少的数据依赖**：缺少后端工作流运行查询 API 挂起或抛出 500 等服务器错误的状态
- **推荐的测试夹具**：需要后端 API 故意在请求 /workflow-runs 时产生数据库连接错误或网络阻断
- **跟进行动**：通过后台触发数据库关闭模拟服务器报错

### 1. [UI-026] 流程中心 / WorkflowRun / 成功详情
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-026 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-027] 流程中心 / WorkflowRun / 运行中详情
- **断言未成立原因**：未能在详情页面中找到 '运行中' 或 步骤执行进度条 等动态刷新提示
- **实际观察状态**：该运行记录在数据库中的最终状态为 succeeded (成功完成)
- **缺少的数据依赖**：缺少处于 queued 或 running 状态下的工作流运行详情页面
- **推荐的测试夹具**：需要启动并维持一个长期处于 running 状态的 workflow_run 运行记录
- **跟进行动**：通过后台接口触发一个模拟 n8n 长耗时运行任务

### 1. [UI-028] 流程中心 / WorkflowRun / 失败详情
- **断言未成立原因**：未能在页面中找到 '运行失败'、'错误诊断信息' 或者是具体的错误调用日志
- **实际观察状态**：页面展示此运行实例已成功 (succeeded)
- **缺少的数据依赖**：缺少处于 failed 状态下的工作流运行详情页面
- **推荐的测试夹具**：需要在数据库中拥有一个 failed 状态且包含错误输出日志的 workflow_run 记录
- **跟进行动**：修改数据库中对应记录的状态为 failed 并注入错误 payload

### 1. [UI-029] 流程中心 / WorkflowRun / Retry 确认弹窗
- **断言未成立原因**：未能在页面上找到 '重试' 按钮或点击后的 Retry 确认弹窗
- **实际观察状态**：当前运行实例为成功态，重试 Retry 入口不可用或被禁用
- **缺少的数据依赖**：缺少处于 failed 状态以激活 Retry 重试按钮入口的运行详情
- **推荐的测试夹具**：需要当前详情页面的运行实例处于 failed 状态以呈现重试操作入口
- **跟进行动**：将运行详情记录的状态变更为失败后，点击重试操作以拉起确认弹窗

### 1. [UI-030] 内置流程 / 旧 Workflows / 只读引导页
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-030 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-032] 章节规划 / 章节规划 / 主线剧情扩展运行中
- **断言未成立原因**：未能在规划页面中看到 '主线剧情扩展'、'运行中' 或生成面板
- **实际观察状态**：页面显示规划状态为空闲，没有任何活跃任务在运行
- **缺少的数据依赖**：缺少正在进行主线剧情大纲扩展的工作流活跃运行实例
- **推荐的测试夹具**：需要在后台启动一个章节规划运行实例并维持在 running 状态
- **跟进行动**：在后台发起章节规划工作流大纲任务

### 1. [UI-033] 章节规划 / 章节规划 / 局部范围生成运行中
- **断言未成立原因**：未能在规划页面中找到 '局部范围生成'、'运行中' 提示
- **实际观察状态**：章节规划状态为空闲
- **缺少的数据依赖**：缺少正在进行局部范围大纲生成的工作流活跃运行实例
- **推荐的测试夹具**：需要发起局部生成任务并保持在 running 状态
- **跟进行动**：选择指定章节范围并发起局部大纲规划生成任务

### 1. [UI-034] 章节规划 / 章节规划 / 完整章节规划运行中
- **断言未成立原因**：未能在页面中找到 '完整章节规划'、'运行中' 提示
- **实际观察状态**：页面章节大纲处于确认空闲状态
- **缺少的数据依赖**：缺少正在进行全章节大纲重新规划的活跃运行实例
- **推荐的测试夹具**：需要发起完整规划大纲生成任务并保持在 running 状态
- **跟进行动**：点击重新生成并选择完整大纲生成任务

### 1. [UI-035] 章节规划 / 章节规划 / 局部生成剩余时间
- **断言未成立原因**：未能在页面中找到 '预计剩余时间' 或具体的秒数倒计时
- **实际观察状态**：章节规划大纲无任务运行，未呈现倒计时
- **缺少的数据依赖**：缺少处于 running 状态且包含预估剩余时间 (ETA) 指标的运行记录
- **推荐的测试夹具**：需要后台工作流返回包含预计执行时间或剩余秒数 (ETA) 数据的运行记录
- **跟进行动**：触发章节大纲生成，并注入包含 ETA 预估时长的进度更新包

### 1. [UI-036] 章节规划 / 章节规划 / 结果校验中
- **断言未成立原因**：未能在页面中找到 '结果校验中' 提示
- **实际观察状态**：页面大纲处于空闲完成状态
- **缺少的数据依赖**：缺少正在进行大纲结果数据校验或可用性预处理的中间态
- **推荐的测试夹具**：需要在生成完毕到写入候选之前，大纲规划运行状态为 validating (校验中)
- **跟进行动**：使大纲生成流在结果提交校验步骤处处于等待挂起状态

### 1. [UI-038] 章节规划 / 章节规划 / 失败且候选零写入
- **断言未成立原因**：未能在页面中看到 '失败' 且提示 '未能生成任何规划候选' 错误信息
- **实际观察状态**：页面呈现健康的规划确认列表，无任何大纲失败警示
- **缺少的数据依赖**：缺少因为后端原子化报错导致没有任何大纲写入的失败大纲规划任务
- **推荐的测试夹具**：需要在数据库中包含一个 failed 状态且没有输出任何大纲章节的最新规划运行记录
- **跟进行动**：制造一个引发异常报错的章节大纲运行实例

### 1. [UI-039] 章节规划 / 章节规划 / 工作流未配置
- **断言未成立原因**：未能在页面中看到 '尚未配置章节规划工作流' 提示及 '前往绑定' 链接
- **实际观察状态**：页面正常渲染了已有的大纲章节列表及已绑定执行入口
- **缺少的数据依赖**：缺少项目未配置或已解除绑定章节规划工作流的状态
- **推荐的测试夹具**：需要删除该项目在 project_workflow_bindings 中关于 chapter_planning 阶段的绑定记录
- **跟进行动**：在项目设置中暂时解绑大纲规划工作流配置

### 1. [UI-040] 章节规划 / 章节规划 / 生成参数抽屉
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-040 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-041] 章节规划 / 章节规划 / 预检进度弹窗
- **断言未成立原因**：未能在页面中找到预检进度弹窗或加载圈
- **实际观察状态**：只读状态下预检已瞬间处理完毕，未捕获到加载中弹窗
- **缺少的数据依赖**：缺少正在发起预检的动态加载加载中面板
- **推荐的测试夹具**：需要在前端发出预检请求且请求尚未返回时，保持网络挂起以截取预检进度
- **跟进行动**：通过调试或慢速网络拦截在发出 preflight 请求时进行抓拍

### 1. [UI-042] 章节规划 / 章节规划 / 预检通过报告
- **断言未成立原因**：未能在页面中看到通过的预检条件明细以及 '配置预检通过' 提示
- **实际观察状态**：只读模式下无法在不真正触发接口写流程时稳定呈现预检通过报告
- **缺少的数据依赖**：缺少显示配置预检通过报告的弹窗面板
- **推荐的测试夹具**：需要在只读状态下成功触发预检检查请求并成功返回通过状态报告
- **跟进行动**：通过页面按钮发起预检，并在弹出的预检结果报告中进行截图

### 1. [UI-043] 章节规划 / 章节规划 / 预检阻断报告
- **断言未成立原因**：未能在页面上找到 '预检未通过'、'未配置关联外部服务' 或者是 '前往配置' 按钮
- **实际观察状态**：项目预检通过，未显示任何阻断阻隔报告
- **缺少的数据依赖**：缺少因为缺少关键连接而触发配置预检阻断的报告面板
- **推荐的测试夹具**：需要使项目的某项全局配置失效，或者解绑定工作流所需的全局 LLM 提供商
- **跟进行动**：暂时清除项目绑定的 LLM Provider 密钥或全局密钥以触发预检阻断

### 1. [UI-044] 章节规划 / 章节规划 / 任务创建成功提示
- **断言未成立原因**：未能在页面中找到 '任务创建成功' 提示
- **实际观察状态**：只读访问大纲规划列表，未发起任何创建操作，无提示横幅
- **缺少的数据依赖**：缺少刚刚成功提交并在后台成功创建生成任务的提示横幅
- **推荐的测试夹具**：需要成功提交大纲生成任务并返回 success，在页面跳转前捕获成功创建横幅
- **跟进行动**：提交生成参数大纲请求，并在提交成功的瞬间捕获横幅

### 1. [UI-045] 章节规划 / 候选批次 / 候选批次列表
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-045 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-046] 章节规划 / 候选批次 / 候选批次详情
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-046 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-047] 章节规划 / 候选批次 / 编辑章节候选抽屉
- **断言未成立原因**：未能在页面上找到 '编辑候选' 或 '编辑' 抽屉操作按钮
- **实际观察状态**：所查看批次的状态已是 adopted (已确认采纳)，所有候选章节均已锁定，编辑功能禁用
- **缺少的数据依赖**：缺少未采纳 (pending) 的候选批次章节以便开启编辑草稿的状态
- **推荐的测试夹具**：需要所查阅的候选批次在数据库中状态为 pending (待确认) 以提供编辑入口
- **跟进行动**：通过后台接口或重新生成创建一个新的 pending 状态的候选大纲批次

### 1. [UI-048] 章节规划 / 候选批次 / 当前章节与候选差异对比
- **断言未成立原因**：未能在页面中唤起 '差异对比' 弹窗，或找不到对比操作按钮
- **实际观察状态**：候选批次已确认采纳 (adopted)，对比按钮被禁用或关闭
- **缺少的数据依赖**：缺少未确认 (pending) 的批次章节以唤起版本差异对比弹窗的状态
- **推荐的测试夹具**：需要该批次候选章节的状态为待处理，提供对比对比操作入口
- **跟进行动**：访问含有 pending 章节的测试批次并点击对比

### 1. [UI-049] 章节规划 / 候选批次 / 批量采用候选确认
- **断言未成立原因**：未能在页面中唤起 '批量采用已选候选' 确认对话框
- **实际观察状态**：批次已经全部采用完成，确认采用入口不可用
- **缺少的数据依赖**：缺少未确认批次下供批量确认采纳的弹窗面板
- **推荐的测试夹具**：需要批次状态为 pending 且具有待采用的章节以唤起采用对话框
- **跟进行动**：在新生成的 pending 批次详情页中点击批量采纳

### 1. [UI-050] 章节规划 / 候选批次 / 放弃候选批次确认
- **断言未成立原因**：未能在页面中唤起 '放弃本批次' 或者是 '丢弃' 的确认对话框
- **实际观察状态**：批次已是最终采用状态，放弃功能不可用
- **缺少的数据依赖**：缺少未确认批次下供批量放弃丢弃候选章节的弹窗面板
- **推荐的测试夹具**：需要批次状态为 pending 且具有待采用章节以唤起放弃确认弹窗
- **跟进行动**：在待确认的批次详情页中点击放弃本批次

### 1. [UI-051] 章节规划 / 候选批次 / 候选基线过期冲突
- **断言未成立原因**：未能在页面中找到 '基线冲突' 或者是 '过期冲突' 等阻断警示
- **实际观察状态**：候选批次处于采用完毕状态，未发生基线过期冲突
- **缺少的数据依赖**：缺少候选章节因为基线冲突而爆发过期无法采用警示弹窗的状态
- **推荐的测试夹具**：需要后台修改或更新项目的当前章节大纲基线，使该候选批次的依赖过期失效
- **跟进行动**：在新批次未采纳时更新项目当前的大纲，从而人为制造基线冲突

### 1. [UI-052] 故事线 / 章节关联 / 章节关联统计只读
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-052 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-053] 正文生成 / 正文编辑器 / 章节目标 Tab
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-053 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-054] 正文生成 / 正文编辑器 / 故事情报 Tab
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-054 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-055] 正文生成 / 正文编辑器 / 素材库 Tab
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-055 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-056] 正文生成 / 正文编辑器 / 生成正文运行前确认抽屉
- **断言未成立原因**：未能在页面上找到并点击 '生成正文' 或展开运行前确认抽屉
- **实际观察状态**：当前章节作品已发布并属于确认态，生成按钮可能处于不可见或禁用状态
- **缺少的数据依赖**：缺少当章节规划已确认但该章节作品正文尚未成功生成/发布时的生成正文入口
- **推荐的测试夹具**：需要正文处于可修改或可重新生成状态，以展示 '生成正文' 按钮并唤起确认抽屉
- **跟进行动**：在后台创建一个新的草稿状态的章节作品

### 1. [UI-057] 正文生成 / 正文编辑器 / 生成正文已填写要求
- **断言未成立原因**：未能在页面中看到已输入创作要求的抽屉内容
- **实际观察状态**：正文生成抽屉未展开
- **缺少的数据依赖**：缺少处于编辑输入状态的正文生成要求抽屉面板
- **推荐的测试夹具**：需要唤起生成确认抽屉，并向其创作要求输入框中填入特定文字
- **跟进行动**：展开正文生成抽屉并模拟用户输入创作参数

### 1. [UI-058] 正文生成 / 正文编辑器 / 正文生成排队中
- **断言未成立原因**：未能在编辑器页面中找到 '排队中' 或 '等待开始生成' 状态标志
- **实际观察状态**：正文生成页面当前没有任何活跃排队任务
- **缺少的数据依赖**：缺少正文生成流程在后台处于 queued 排队中状态的详情面板
- **推荐的测试夹具**：需要在后台将正文生成工作流运行记录的状态维持在 queued (排队中)
- **跟进行动**：发起正文生成任务并将后台执行器暂时挂起

### 1. [UI-059] 正文生成 / 正文编辑器 / 正文生成运行中
- **断言未成立原因**：未能在编辑器页面中找到 '运行中' 或者是 正处于运行状态的指示
- **实际观察状态**：正文生成页面当前无任何生成流程在运行
- **缺少的数据依赖**：缺少正文生成流程在后台处于 running 运行中状态的详情面板
- **推荐的测试夹具**：需要在后台将正文生成工作流运行记录的状态维持在 running (运行中)
- **跟进行动**：发起正文生成任务，使工作流组件在耗时大模型执行阶段停留

### 1. [UI-060] 正文生成 / 正文编辑器 / 正文候选已创建
- **断言未成立原因**：未能在编辑器页面中找到 '查看候选版本' 或者是 '已就绪' 等采纳操作入口
- **实际观察状态**：作品没有新生成的候选版本，正文已是最终采纳确认状态
- **缺少的数据依赖**：缺少正文生成完成后、用户采纳前，存在待采纳候选版本的状态
- **推荐的测试夹具**：需要工作流成功生成一个 content_version 候选版本，且其状态为待审核采纳
- **跟进行动**：为当前作品发布一个尚未 adopted 的生成候选版本

### 1. [UI-061] 正文生成 / 正文编辑器 / 正文生成失败
- **断言未成立原因**：未能在编辑器页面中看到 '生成失败'、'外部工作流调用超时' 或重试选项
- **实际观察状态**：编辑器没有任何失败的生成记录
- **缺少的数据依赖**：缺少正文生成流程执行失败以显示失败详情和错误日志的状态
- **推荐的测试夹具**：需要工作流运行失败并在数据库中保留 status = failed 且带有错误信息的运行记录
- **跟进行动**：制造一个抛出接口异常的生成任务并记录失败日志

### 1. [UI-062] 正文生成 / 正文编辑器 / 正文候选版本比较
- **断言未成立原因**：未能在页面中找到版本差异对比弹窗
- **实际观察状态**：正文无可对比候选，版本对比入口不可用
- **缺少的数据依赖**：缺少存在生成候选时以供版本差异对比的弹窗面板
- **推荐的测试夹具**：需要作品下挂载一个未采纳的候选版本，以提供版本对比的对比入口
- **跟进行动**：新建一个正文生成候选版本，并在编辑器中点击对比按钮

### 1. [UI-063] 正文生成 / 正文编辑器 / 正文生成工作流未配置
- **断言未成立原因**：未能在页面中看到 '尚未配置正文生成工作流' 和 '前往绑定' 按钮
- **实际观察状态**：正文生成页面已正常绑定工作流配置并支持编辑器加载
- **缺少的数据依赖**：缺少项目未配置或已解除绑定正文生成工作流的页面状态
- **推荐的测试夹具**：需要删除该项目在 project_workflow_bindings 中关于 content_generation 的绑定记录
- **跟进行动**：在项目设置中解绑正文生成阶段的工作流

### 1. [UI-064] 内容审核 / 审核入口 / 正文编辑器提交审核入口
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-064 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-065] 内容审核 / 审核入口 / 发起内容审核抽屉
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-065 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-066] 内容审核 / 审核工作区 / 内容审核运行中
- **断言未成立原因**：未能在审核页面中找到 '正在执行内容审核' 或者是进度运行提示条
- **实际观察状态**：审核工作区无任何活跃审核流在运行，显示最新的已完成审核报告
- **缺少的数据依赖**：缺少内容质量审核流程在后台处于 running 运行中状态的详情面板
- **推荐的测试夹具**：需要在后台将审核工作流运行记录的状态维持在 running (运行中)
- **跟进行动**：触发内容审核任务并将后台流程挂起

### 1. [UI-067] 内容审核 / 审核工作区 / 审核结果总览
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-067 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-068] 内容审核 / 审核工作区 / 问题详情与全文定位
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-068 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-069] 内容审核 / 审核工作区 / 审核任务失败
- **断言未成立原因**：未能在页面中看到 '审核执行失败' 或者是错误提示面板
- **实际观察状态**：审核工作区展示了已完成的历史审核报告，无失败记录
- **缺少的数据依赖**：缺少内容质量审核流程执行失败以显示失败警示的状态
- **推荐的测试夹具**：需要在数据库中保留 status = failed 的内容审核工作流运行记录
- **跟进行动**：制造一个抛出调用异常的质量审核运行记录

### 1. [UI-070] 内容审核 / 审核工作区 / 审核工作流未配置
- **断言未成立原因**：未能在页面中看到 '尚未配置审核工作流' 和 '前往绑定' 的空状态说明
- **实际观察状态**：审核工作区已绑定工作流且能正常运行和显示报告
- **缺少的数据依赖**：缺少项目未配置或已解除绑定内容质量审核工作流的页面状态
- **推荐的测试夹具**：需要删除该项目在 project_workflow_bindings 中关于 review 的绑定记录
- **跟进行动**：在项目设置中解绑内容质量审核阶段的工作流

### 1. [UI-071] 内容审核 / 审核工作区 / 审核历史
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-071 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-072] 正文重写 / 重写工作区 / 选择问题并创建重写入口
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-072 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-073] 正文重写 / 重写工作区 / 创建正文重写
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-073 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-074] 正文重写 / 重写工作区 / 查看项目重写配置抽屉
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-074 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-075] 正文重写 / 重写工作区 / 正文重写运行中
- **断言未成立原因**：未能在页面中找到 '重写执行中' 或者是重写运行中加载提示
- **实际观察状态**：页面呈现重写成功的最终版本详情，无活跃改写流在运行
- **缺少的数据依赖**：缺少正文改写/重写流程在后台处于 running 运行中状态的详情面板
- **推荐的测试夹具**：需要在后台将重写工作流运行记录的状态维持在 running (运行中)
- **跟进行动**：发起正文改写任务并将后台流程拦截挂起

### 1. [UI-076] 正文重写 / 重写工作区 / 正文重写成功候选
- **断言未成立原因**：页面未能在无 Mock 拦截的纯实盘环境下呈现对应状态
- **实际观察状态**：页面呈现出默认的状态，未展示异常/特定的流程面板
- **缺少的数据依赖**：缺少用于触发 UI-076 目标状态所需的特定后端数据条件
- **推荐的测试夹具**：需要在数据库中预先配置好代表该特定状态的测试夹具
- **跟进行动**：往真实数据库注入数据或在流程执行时截获中间过渡态

### 1. [UI-077] 正文重写 / 重写工作区 / 设为当前版本确认
- **断言未成立原因**：未能在页面中唤起 '设为当前版本' 确认弹窗
- **实际观察状态**：改写结果已经处理完成，确认设为当前版本入口已失效
- **缺少的数据依赖**：缺少在重写候选版本发布后供采纳确认的弹窗面板
- **推荐的测试夹具**：需要该改写批次状态处于 pending 以提供设为当前版本操作入口
- **跟进行动**：新建一个重写候选实例并在重置后点击设为当前版本

### 1. [UI-078] 正文重写 / 重写工作区 / 重写结果提交失败
- **断言未成立原因**：未能在页面上找到 '重写结果消费失败' 或者是 '版本写入异常' 提示
- **实际观察状态**：重写结果正常呈现，没有任何结果消费写入报错
- **缺少的数据依赖**：缺少重写成功但在写入/发布当前版本时发生数据提交异常的状态
- **推荐的测试夹具**：需要在重写工作流运行成功后，在执行结果消费逻辑时抛出数据库写异常
- **跟进行动**：使后端在处理重写结果发布时故意发生乐观锁或主键写冲突

### 1. [UI-079] 正文重写 / 重写工作区 / 重写任务执行失败
- **断言未成立原因**：未能在重写页面中看到 '重写执行失败' 或者是 '外部工作流执行出错' 提示
- **实际观察状态**：重写页面正常渲染了成功的重写版本，无失败记录
- **缺少的数据依赖**：缺少重写流程执行失败以显示失败警示和错误日志的状态
- **推荐的测试夹具**：需要在数据库中保留 status = failed 且包含错误输出日志的重写工作流记录
- **跟进行动**：制造一个引发异常报错的重写运行实例

### 1. [UI-080] 正文重写 / 重写工作区 / 未配置/配置失效/空状态
- **断言未成立原因**：未能在页面中看到 '重写功能当前不可用' 或者是 '请前往绑定' 的警示说明
- **实际观察状态**：重写页面功能正常加载并完美支持改写工作流的选用
- **缺少的数据依赖**：缺少项目未配置或已解除绑定正文重写工作流的页面状态
- **推荐的测试夹具**：需要删除该项目在 project_workflow_bindings 中关于 rewrite 的绑定记录
- **跟进行动**：在项目设置中解除绑定正文重写阶段的工作流

### 1. [UI-081] 共享升级状态 / 章节规划 / 共享预检阻断
- **断言未成立原因**：未能在页面中看到 '前置预检未通过'、'未配置关联外部服务' 提示
- **实际观察状态**：大纲预检顺利通过，页面呈现正常的章节列表
- **缺少的数据依赖**：缺少触发共享预检阻断的错误连接大纲配置状态
- **推荐的测试夹具**：需要项目在进行大纲章节生成前，其预检机制由于缺少关键连接而触发阻断
- **跟进行动**：暂时删除大纲规划工作流对应的全局 LLM 服务商以唤醒共享阻断

### 1. [UI-082] 共享升级状态 / 正文生成 / 共享预检阻断
- **断言未成立原因**：未能在页面中看到 '前置预检未通过' 提示
- **实际观察状态**：正文编辑器加载正常，生成预检顺利通过
- **缺少的数据依赖**：缺少触发共享预检阻断的正文生成环境状态
- **推荐的测试夹具**：需要项目在正文生成前其预检机制由于配置失效而发生阻断
- **跟进行动**：暂时删除正文生成配置以唤醒阻断

### 1. [UI-083] 共享升级状态 / 内容审核 / 共享预检阻断
- **断言未成立原因**：未能在页面中看到 '前置预检未通过' 提示
- **实际观察状态**：审核工作区加载正常，审核预检顺利通过
- **缺少的数据依赖**：缺少触发共享预检阻断的审核环境状态
- **推荐的测试夹具**：需要项目在内容审核前其预检机制由于配置失效而发生阻断
- **跟进行动**：暂时解绑内容审核工作流以唤醒阻断

### 1. [UI-084] 共享升级状态 / 正文重写 / 共享预检阻断
- **断言未成立原因**：未能在页面中看到 '前置预检未通过' 提示
- **实际观察状态**：重写功能正常加载，未见预检阻断阻隔
- **缺少的数据依赖**：缺少触发共享预检阻断的重写环境状态
- **推荐的测试夹具**：需要项目在正文重写前其预检机制由于配置失效而发生阻断
- **跟进行动**：暂时解绑正文重写工作流以唤醒阻断

### 1. [UI-085] 共享升级状态 / 章节规划 / 共享运行恢复
- **断言未成立原因**：未能在页面中找到 '任务执行异常中断'、'故障诊断信息' 或恢复建议
- **实际观察状态**：大纲章节规划列表一切正常，无任何活跃或异常任务指示
- **缺少的数据依赖**：缺少处于故障或中断状态的大纲规划运行实例以激活故障诊断面板
- **推荐的测试夹具**：需要最近的大纲规划运行实例被标记为异常中断 (interrupted) 以展示共享诊断界面
- **跟进行动**：往数据库中写入一条 status = interrupted 的大纲规划运行记录

### 1. [UI-086] 共享升级状态 / 正文生成 / 共享运行恢复
- **断言未成立原因**：未能在页面中找到 '任务执行异常中断'、'故障诊断信息' 或者是重试恢复指示
- **实际观察状态**：作品正文生成一切空闲或成功，未进入共享运行恢复状态
- **缺少的数据依赖**：缺少处于故障或中断状态的正文生成运行实例以展示恢复面板
- **推荐的测试夹具**：需要最近的正文生成运行记录被标记为异常中断 (interrupted) 以拉起正文恢复面板
- **跟进行动**：往数据库中写入一条 status = interrupted 的正文生成运行记录

### 1. [UI-087] 共享升级状态 / 内容审核 / 共享运行恢复
- **断言未成立原因**：未能在页面中找到 '任务执行异常中断'、'故障诊断信息' 提示
- **实际观察状态**：内容审核工作区未进入故障运行恢复状态
- **缺少的数据依赖**：缺少处于故障或中断状态的内容审核运行实例以展示恢复面板
- **推荐的测试夹具**：需要最近的内容审核运行记录被标记为异常中断 (interrupted) 以拉起审核恢复面板
- **跟进行动**：往数据库中写入一条 status = interrupted 的内容审核运行记录

### 1. [UI-088] 共享升级状态 / 正文重写 / 共享运行恢复
- **断言未成立原因**：未能在页面中找到 '任务执行异常中断'、'故障诊断信息' 提示
- **实际观察状态**：改写页面未进入故障运行恢复状态
- **缺少的数据依赖**：缺少处于故障或中断状态的正文重写运行实例以展示恢复面板
- **推荐的测试夹具**：需要最近的正文重写运行记录被标记为异常中断 (interrupted) 以拉起重写恢复面板
- **跟进行动**：往数据库中写入一条 status = interrupted 的改写重写运行记录

