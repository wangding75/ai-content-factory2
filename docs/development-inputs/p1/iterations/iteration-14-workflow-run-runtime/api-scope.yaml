iteration: iteration-14-workflow-run-runtime
status: planned
apis:
  - "GET /api/v1/workflow-runs"
  - "GET /workflow-runs/{id}"
  - "GET /workflow-runs/{id}/events"
  - "POST /workflow-runs/{id}/retries"
  - "POST /workflow-runs/{id}/domain-commit-retries"
common_requirements:
  - OpenAPI is the single contract source
  - all commands use Idempotency-Key where applicable
  - errors use the shared error envelope
  - secrets and raw upstream responses are never returned
  - optimistic locking is required for mutable configuration
