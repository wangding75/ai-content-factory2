import {AppShell} from "@/components/ui/app-shell";import {RewriteWorkspace} from "@/features/project-works/rewrite-workspace";
export default async function RewriteRoute({params,searchParams}:{params:Promise<{projectId:string;workId:string}>;searchParams:Promise<Record<string,string|undefined>>}){const {projectId,workId}=await params;return <AppShell active="projects"><RewriteWorkspace projectId={projectId} workId={workId} search={await searchParams}/></AppShell>}
