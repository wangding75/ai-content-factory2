import {AppShell} from "@/components/ui/app-shell";import {RewriteWorkspace} from "@/features/project-works/rewrite-workspace";
export default async function RewriteRoute({params,searchParams}:{params:Promise<{projectId:string;workId:string}>;searchParams:Promise<{reportId?:string;workflowRunId?:string;history?:string}>}){const {projectId,workId}=await params;const search=await searchParams;return <AppShell active="projects"><RewriteWorkspace projectId={projectId} workId={workId} reportId={search.reportId} workflowRunId={search.workflowRunId} showHistory={search.history==="1"}/></AppShell>}
