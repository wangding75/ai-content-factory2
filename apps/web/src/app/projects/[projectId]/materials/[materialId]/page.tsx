import {AppShell} from "@/components/ui/app-shell";import {parsePlanningMockScenario} from "@/features/planning-materials/api/planning-api";import {MaterialsPage} from "@/features/planning-materials/materials/materials-page";import {MaterialDetailDrawer} from "@/features/planning-materials/materials/material-detail-drawer";export default async function Page({params,searchParams}:{params:Promise<{projectId:string;materialId:string}>;searchParams:Promise<{mockScenario?:string}>}){const {projectId,materialId}=await params;const {mockScenario}=await searchParams;const scenario=parsePlanningMockScenario(mockScenario);return <AppShell active="projects"><MaterialsPage projectId={projectId} scenario={scenario}/><MaterialDetailDrawer projectId={projectId} materialId={materialId} scenario={scenario}/></AppShell>}
