import {AppShell} from "@/components/ui/app-shell";import {parsePlanningMockScenario} from "@/features/planning-materials/api/planning-api";import {MaterialsPage} from "@/features/planning-materials/materials/materials-page";import {CreateMaterialPage} from "@/features/planning-materials/materials/create-material-page";export default async function Page({params,searchParams}:{params:Promise<{projectId:string}>;searchParams:Promise<{mockScenario?:string}>}){const {projectId}=await params;const {mockScenario}=await searchParams;const scenario=parsePlanningMockScenario(mockScenario);return <AppShell active="projects"><MaterialsPage projectId={projectId} scenario={scenario}/><CreateMaterialPage projectId={projectId} scenario={scenario}/></AppShell>}
