import assert from"node:assert/strict";import test from"node:test";import{updatePayload,validate,type Form}from"./distribution-api.ts";const f:Form={name:"p",platformType:"custom",accountIdentifier:"a",endpointUrl:"",authType:"api_key",timeoutSeconds:30,typeConfigJson:"{}",note:"",credential:""};test("validates JSON and excludes readonly platform type on update",()=>{assert.match(validate({...f,typeConfigJson:"{"},true)??"",/JSON/);assert.equal("platformType"in updatePayload(f,1),false);});
