import assert from "node:assert/strict";
import test from "node:test";
import { createConnectionPayload, mapConnection, updateConnectionPayload, validateConnectionForm } from "./workflow-connection-api.ts";
const types=[{connectionType:"n8n",displayName:"n8n",authTypes:["api_key"] as ["api_key"],fieldSchemas:[]}];const dto={id:"a",name:"main",connectionType:"n8n",baseUrl:"https://n8n.test",authType:"api_key" as const,timeoutSeconds:30,typeConfig:{referenceType:"workflow_id" as const,referenceValue:"x"},hasCredential:true,credentialFingerprint:"safe",integrationStatus:"not_connected" as const,enabled:false,lastVerifiedAt:null,lastErrorCode:null,lastErrorMessage:null,version:2,createdAt:"",updatedAt:""};const form={name:"main",connectionType:"n8n",baseUrl:"https://n8n.test",timeoutSeconds:30,typeConfigJson:'{"referenceType":"workflow_id","referenceValue":"x"}',credential:"input-only"};
test("maps type catalogue and response data without credentials",()=>{const vm=mapConnection(dto,types);assert.equal(vm.typeLabel,"n8n");assert.equal("credential" in vm,false);});
test("create and update requests keep contract write boundaries",()=>{assert.equal(createConnectionPayload(form).connectionType,"n8n");assert.deepEqual(updateConnectionPayload({...form,expectedVersion:2,credential:""}),{expectedVersion:2,name:"main",baseUrl:"https://n8n.test",authType:"api_key",timeoutSeconds:30,typeConfig:dto.typeConfig});});
test("invalid JSON blocks submission and 409 is handled by page state",()=>{assert.match(validateConnectionForm({...form,typeConfigJson:"{"},false)??"",/JSON/);});
