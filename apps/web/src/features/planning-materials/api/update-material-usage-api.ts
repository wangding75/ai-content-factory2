import {ApiError} from "../../../lib/api.ts";import type {ProjectMaterialUsageInput} from "../contracts/materials.ts";import {findGlobalMaterial,updateStoredUsage} from "../mock/material-repository.ts";const uuid=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;export type UpdateProjectMaterialUsageRequest=Partial<ProjectMaterialUsageInput>&{expected_version:number};export async function updateProjectMaterialUsage(p:string,m:string,r:UpdateProjectMaterialUsageRequest,s="default"){if(!uuid.test(p))throw new ApiError("项目地址无效。",400,"INVALID_PROJECT_ID");if(!uuid.test(m))throw new ApiError("素材地址无效。",400,"INVALID_MATERIAL_ID");if(!findGlobalMaterial(m))throw new ApiError("素材不存在。",404,"MATERIAL_NOT_FOUND");if(s==="save-error")throw new ApiError("保存项目用途失败。",500,"USAGE_SAVE_FAILED");if(s==="conflict")throw new ApiError("项目用途已在其他位置更新。",409,"VERSION_CONFLICT");const {expected_version,...patch}=r;if(!Object.keys(patch).length)throw new ApiError("至少修改一个用途字段。",400,"VALIDATION_ERROR");const x=updateStoredUsage(p,m,patch,expected_version);if(x===undefined)throw new ApiError("当前项目已不再绑定该素材。",404,"MATERIAL_BINDING_NOT_FOUND");if(x===null)throw new ApiError("项目用途已在其他位置更新。",409,"VERSION_CONFLICT");return {data:x,request_id:"req_usage"}}
