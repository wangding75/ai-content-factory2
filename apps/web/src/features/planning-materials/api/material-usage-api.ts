import {ApiError} from "../../../lib/api.ts";import {findGlobalMaterial,listStoredProjectMaterials} from "../mock/material-repository.ts";const uuid=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;export async function getProjectMaterialUsage(projectId:string,materialId:string,scenario:string="default"){if(scenario==="slow")await new Promise(r=>setTimeout(r,900));if(!uuid.test(projectId))throw new ApiError("项目地址无效。",400,"INVALID_PROJECT_ID");if(!uuid.test(materialId))throw new ApiError("素材地址无效。",400,"INVALID_MATERIAL_ID");if(scenario==="load-error")throw new ApiError("暂时无法加载项目素材用途。",500,"USAGE_LOAD_FAILED");if(scenario==="not-found")throw new ApiError("项目不存在。",404,"PROJECT_NOT_FOUND");const material=findGlobalMaterial(materialId);if(!material)throw new ApiError("素材不存在。",404,"MATERIAL_NOT_FOUND");const item=listStoredProjectMaterials().find(x=>x.usage.project_id===projectId&&x.material.id===materialId&&x.usage.status==="active");if(!item||scenario==="binding-not-found")throw new ApiError("当前项目已不再绑定该素材。",404,"MATERIAL_BINDING_NOT_FOUND");return item}
