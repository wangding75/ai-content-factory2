import {ApiError} from "../../../lib/api.ts";import {findGlobalMaterial,unbindStoredMaterial} from "../mock/material-repository.ts";const uuid=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;export async function unbindProjectMaterial(projectId:string,materialId:string,expectedVersion:number,scenario:string="default"){if(!uuid.test(projectId))throw new ApiError("项目地址无效。",400,"INVALID_PROJECT_ID");if(!uuid.test(materialId))throw new ApiError("素材地址无效。",400,"INVALID_MATERIAL_ID");if(scenario==="not-found")throw new ApiError("项目不存在。",404,"PROJECT_NOT_FOUND");if(!findGlobalMaterial(materialId))throw new ApiError("素材不存在。",404,"MATERIAL_NOT_FOUND");if(scenario==="unbind-error")throw new ApiError("解除绑定失败。",500,"UNBIND_FAILED");if(scenario==="conflict")throw new ApiError("绑定已被其他位置更新。",409,"VERSION_CONFLICT");const result=scenario==="already-unbound"?false:unbindStoredMaterial(projectId,materialId,expectedVersion);if(result===null)throw new ApiError("绑定已被其他位置更新。",409,"VERSION_CONFLICT");return {data:{project_id:projectId,material_id:materialId,unbound:result,material_retained:true},request_id:"req_unbind"}}
