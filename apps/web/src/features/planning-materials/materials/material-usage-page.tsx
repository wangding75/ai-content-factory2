"use client";import Link from "next/link";import {useRouter} from "next/navigation";import {useCallback,useEffect,useState} from "react";import {updateProjectMaterialUsage} from "../api/update-material-usage-api";import {getProjectMaterialUsage} from "../api/material-usage-api";import {ProjectWorkspaceNav} from "../components/project-workspace-nav";import {useLayerInteractions} from "../components/layer-interactions";import {closeMaterialLayer} from "../components/material-layer-routes";import type {ProjectMaterialItem} from "../contracts/materials";import type {PlanningMockScenario} from "../contracts/planning";const labels={character:"人物",worldview:"世界观",location:"地点",organization:"组织",item:"道具",reference:"参考资料"};export function MaterialUsagePage({projectId,materialId,scenario}:{projectId:string;materialId:string;scenario:PlanningMockScenario}){const router=useRouter();const close=useCallback(()=>router.push(closeMaterialLayer("usage",projectId,materialId)),[router,projectId,materialId]);const layerRef=useLayerInteractions<HTMLDivElement>(close);const [item,setItem]=useState<ProjectMaterialItem|null>(null);const [error,setError]=useState("");const [form,setForm]=useState<ProjectMaterialItem["usage"]|null>(null);const [saving,setSaving]=useState(false);const load=useCallback(async()=>{try{setError("");const next=await getProjectMaterialUsage(projectId,materialId,scenario);setItem(next);setForm(next.usage)}catch(e){setError(e instanceof Error?e.message:"加载失败")}},[projectId,materialId,scenario]);useEffect(()=>{const t=setTimeout(()=>void load(),0);return()=>clearTimeout(t)},[load]);if(error)return <main className="materials-state"><h1>{error.includes("绑定")?"当前项目未绑定该素材":"项目素材用途加载失败"}</h1><p>{error}</p><div><button onClick={load}>重试</button><Link href={`/projects/${projectId}/materials/${materialId}`}>返回素材详情</Link></div></main>;if(!item||!form)return <div className="detail-skeleton"/>;const {material,usage}=item;const dirty=JSON.stringify(form)!==JSON.stringify(usage);const save=async()=>{setSaving(true);try{const r=await updateProjectMaterialUsage(projectId,materialId,{expected_version:usage.version,usage_type:form.usage_type,role_name:form.role_name,notes:form.notes,start_chapter:form.start_chapter,end_chapter:form.end_chapter},scenario);setItem(r.data);setForm(r.data.usage);router.push(closeMaterialLayer("usage",projectId,materialId))}catch(e){setError(e instanceof Error?e.message:"保存失败")}finally{setSaving(false)}};return <div ref={layerRef} className="create-material" role="dialog" aria-modal="true" aria-label="编辑项目素材用途" tabIndex={-1}><header><p>项目 / 编辑素材用途</p><h1>编辑项目素材用途</h1></header><ProjectWorkspaceNav projectId={projectId} active="materials"/><main><div className="create-modal"><section className="create-head"><div><h2>{material.name}</h2><p>{labels[material.type]} · {material.summary||"暂无简介"}</p></div></section><div className="create-body"><p className="create-tip">当前页面只影响本项目用途，不会修改全局素材，也不会影响其他项目。</p><label className="create-field"><span>项目用途</span><input value={form.usage_type} onChange={e=>setForm({...form,usage_type:e.target.value})}/></label><label className="create-field"><span>角色名称</span><input value={form.role_name} onChange={e=>setForm({...form,role_name:e.target.value})}/></label><label className="create-field"><span>使用说明</span><textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label><div className="create-grid"><label className="create-field"><span>起始章节</span><input type="number" value={form.start_chapter??""} onChange={e=>setForm({...form,start_chapter:e.target.value?Number(e.target.value):null})}/></label><label className="create-field"><span>结束章节</span><input type="number" value={form.end_chapter??""} onChange={e=>setForm({...form,end_chapter:e.target.value?Number(e.target.value):null})}/></label></div><p className="detail-label">当前 Usage 版本：v{usage.version}</p></div><footer><Link href={`/projects/${projectId}/materials/${materialId}`}>取消</Link><button disabled={!dirty||saving} onClick={save}>{saving?"保存中…":"保存用途"}</button></footer></div></main></div>}
