import {ApiError} from "@/lib/api";import type {RewriteContext,RewriteForm,RewriteMockState,RewriteResult} from "./rewrite-api";
const fail=(state:RewriteMockState)=>{const map:Partial<Record<RewriteMockState,[number,string,string]>>={"invalid-parameters":[422,"invalid_rewrite_parameters","重写参数无效。"],stale:[409,"version_conflict","来源版本已变化，请返回作品列表刷新后重试。"],"idempotency-conflict":[409,"idempotency_key_reused_with_different_payload","该提交键已用于不同请求，请修改参数后重新提交。"],"already-exists":[409,"rewrite_already_exists","该正文已存在重写版本。"],"source-not-frozen":[409,"content_version_not_frozen","来源版本尚未冻结，不能创建重写。"],"review-not-completed":[409,"review_not_completed","来源审核尚未完成。"],"relation-mismatch":[409,"source_version_mismatch","来源版本与审核结果不匹配。"],"provider-failure":[500,"mock_rewrite_failed","模拟重写失败，请稍后使用新提交重试。"],"internal-error":[500,"internal_error","暂时无法创建重写，请稍后重试。"]};return map[state]};
export async function mockRewrite(context:RewriteContext,form:RewriteForm,key:string,state:RewriteMockState):Promise<RewriteResult>{if(state==="loading")return new Promise(()=>{});const f=fail(state);if(f)throw new ApiError(f[2],f[0],f[1]);return {source_content_version_id:context.sourceVersionId,target_content_version_id:"66666666-6666-4666-8666-666666666666",target_version_no:2,source:"mock_rewrite",current_version_id_unchanged:true,workflow_run:{id:"77777777-7777-4777-8777-777777777777",status:"succeeded",target_content_version_id:"66666666-6666-4666-8666-666666666666"},idempotent_replay:false};}
