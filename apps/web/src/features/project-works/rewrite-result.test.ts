import test from "node:test";import assert from "node:assert/strict";import{readFileSync}from"node:fs";
test("D5 validates real IDs and refreshes WorkflowRun",()=>{const s=readFileSync(new URL("./rewrite-result-workspace.tsx",import.meta.url),"utf8");for(const x of ["sourceVersionId","targetVersionId","workflowRunId","succeeded","running","failed","getWorkflowRun","getContentVersion","AbortController"])assert.match(s,new RegExp(x));assert.doesNotMatch(s,/href="#"/)});
