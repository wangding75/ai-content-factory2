import test from "node:test";import assert from "node:assert/strict";import{readFileSync}from"node:fs";test("D5 validates frozen ids and preserves v1 current",()=>{const s=readFileSync(new URL("./rewrite-result-workspace.tsx",import.meta.url),"utf8");for(const x of ["sourceVersionId","targetVersionId","workflowRunId","succeeded","running","failed","当前版本仍为 v1"])assert.match(s,new RegExp(x));assert.doesNotMatch(s,/href="#"/)});
